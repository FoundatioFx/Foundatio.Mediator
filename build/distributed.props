<Project>

  <!-- Distributed packages version with the repository through MinVer so distributed prerelease policy does not change core package versions. -->
  <PropertyGroup>
    <MinVerDefaultPreReleaseIdentifiers>beta.0</MinVerDefaultPreReleaseIdentifiers>
    <PackageTags>$(PackageTags);Distributed</PackageTags>
  </PropertyGroup>

</Project>
