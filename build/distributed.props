<Project>

  <!-- Distributed packages version with the repository through MinVer so every branch build publishes a new preview. -->
  <PropertyGroup>
    <PackageTags>$(PackageTags);Distributed</PackageTags>
  </PropertyGroup>

</Project>
