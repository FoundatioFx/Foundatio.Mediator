<Project Sdk="Microsoft.NET.Sdk">
  <Import Project="..\..\build\common.props" />
  <Import Project="..\..\build\distributed.props" />
  <Import Project="..\..\build\foundatio-core.props" />

  <PropertyGroup>
    <!-- Source experiment: enable packaging after the pinned native core APIs are released. -->
    <IsPackable>false</IsPackable>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <RootNamespace>Foundatio.Mediator.Distributed</RootNamespace>
    <Description>Durable queues, distributed notifications, and background workers for Foundatio.Mediator. Route a handler through a queue with [Queue]; select which workers a process runs with one setting.</Description>
    <PackageTags>$(PackageTags);Queue;Worker;PubSub;Distributed</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Foundatio.Mediator.Abstractions\Foundatio.Mediator.Abstractions.csproj" />
    <ProjectReference Include="..\Foundatio.Mediator\Foundatio.Mediator.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.11" />
  </ItemGroup>

</Project>
